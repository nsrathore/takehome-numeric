# SQLite by default -- zero setup, works out of the box.
# Swap to Postgres for anything closer to production:
#   DATABASE_URL="postgresql://user:password@localhost:5432/takehome?schema=public"
DATABASE_URL="file:./dev.db"
